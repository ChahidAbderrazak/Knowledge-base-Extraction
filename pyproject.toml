[tool.ruff]
line-length = 180
exclude = [
  ".venv*",
  "*.ipynb",
  "cache/*",
  "dev/*",
]

[tool.isort]
profile = "black"
line_length = 180
include_trailing_comma = true
multi_line_output = 3
force_grid_wrap = 0
use_parentheses = true